version: "3"
services:
  drainpipe:
    build:
      dockerfile: ./apps/drainpipe/Dockerfile
      context: ../../
    env_file:
      - ./.env.local
    volumes:
      - ./drainpipedata:/drainpipedata
