export const metadata = {
  title: "Threaded Forum • Frontpage Design System",
};

export default function ThreadedForumLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
